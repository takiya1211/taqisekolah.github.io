document.getElementById('menuToggle').addEventListener('click', function() {
    const menu = document.querySelector('.taqi_menu');
    menu.classList.toggle('show'); // Toggles the visibility of the menu
});